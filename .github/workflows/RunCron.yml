name:
  RunCron

on:
  schedule:
    - cron: '*/5 * * * *'
  push:
    branches:
      - 'runningPushTest'

jobs:
  runcron:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
        with:
          ref: 'master'
      - uses: actions/setup-node@v1
        with:
          node-version: '16.x'
      - run: npm install
      - run: npm run build
      - run: npm run start
        env:
          ABCDEFG: ${{ secrets.ABCDEFG }}
          OWNER: ${{ secrets.OWNER }}
          REPO: ${{ secrets.REPO }}
